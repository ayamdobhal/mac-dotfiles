# Placeholder so the flake evaluates on the Mac. On the ThinkPad live ISO:
#   nixos-generate-config --root /mnt
#   cp /mnt/etc/nixos/hardware-configuration.nix \
#      /mnt/etc/nixos/nix-config/hosts/thonkpad/hardware-configuration.nix
#   git -C /mnt/etc/nixos/nix-config add hosts/thonkpad/hardware-configuration.nix
{ ... }:

{
}
