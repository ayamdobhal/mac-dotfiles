dev-recap
cabal
raycast/extensions
invideo
nix-config/result

# Home-manager managed paths — symlinks point into /nix/store, hash churns on every rebuild
git/config
direnv/lib/hm-nix-direnv.sh
starship.toml
**/*.backup
